# Node B — reasoning + escalation (Mistral, Qdrant, escalation policy)

include "application.conf"

akka {
  remote.artery {
    canonical.hostname = ${safety.cluster.host-bc}
    canonical.port = 2552
  }

  cluster {
    roles = ["reasoning", "escalation", "persistence"]
  }
}
